String // stores text. They're surrounded by double quotes
String myText = "Hello";
int // stores integers(whole numbers) without decimals
int myNum = 5;
float // stores floating point numbers with decimals
float myFloatNum = 5.99f;
char // stores single characters such as 'a' or 'B' surrounded by single quotes
char myLetter = 'D';
boolean // stores values with 2 states: true and false
boolean myBool = true;
final before any variabele // use if you don't want anyone to overwrite existing values (read-only)
final int myNum = 15;

// multiple of the same variable are separated by commas (,)
